resources:
  pipelines:
    bronze_pipeline:
      # name: PL_bronze_ingestion_${bundle.target}

      libraries:
        - file:
            path: ../src/DAB_etl/etl_bronze.py

      schema: bronze_stg
      catalog: surakshadb

      continuous: false
      photon: true
      serverless: true
