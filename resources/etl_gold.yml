resources:
  pipelines:
    gold_pipeline:
      name: PL_gold_BI_${bundle.target}

      libraries:
        - file:
            path: ../src/DAB_etl/etl_bronze.py
      
      catalog: ${var.catalog}
      target: ${var.pipeline_schema}
      
      continuous: false
      photon: true 
      serverless: true